config BR2_PACKAGE_RETROARCH
	bool "retroarch"
	depends on BR2_PACKAGE_GAMEJAY_PREBUILT_BASE
	depends on BR2_TOOLCHAIN_HAS_THREADS
	help
	  RetroArch libretro frontend configured for DRM/KMS, SDL2, ALSA,
	  and udev.
